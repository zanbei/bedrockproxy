import { ISDK } from '../aws-auth/sdk';
export declare function determineAllowCrossAccountAssetPublishing(sdk: ISDK, customStackName?: string): Promise<boolean>;
interface BootstrapStackInfo {
    hasStagingBucket: boolean;
    bootstrapVersion: number;
}
export declare function getBootstrapStackInfo(sdk: ISDK, stackName: string): Promise<BootstrapStackInfo>;
export {};
